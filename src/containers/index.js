import PhotoContainer from './photo/photo-container'
import DonutContainer from './donut/donut-container'
import WeatherContainer from './weather/weather-container'
import CityContainer from './city/city-container'

const containers = {
    PhotoContainer: PhotoContainer,
    DonutContainer: DonutContainer,
    WeatherContainer: WeatherContainer,
    CityContainer: CityContainer
}

export default containers
